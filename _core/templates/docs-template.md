---
tags: [docs, "{{STAGE_TAG}}", "{{TOPIC_TAG}}"]
created: "{{CREATED_DATE}}"
updated: "{{UPDATED_DATE}}"
---

# {{TOPIC_TITLE}}

## 1. 개요

{{TOPIC_TITLE}}의 역할과 이 프로젝트에서 담당하는 책임을 2~4문장으로 서술한다.
전체 data flow에서 어느 위치인지, 무엇을 입력으로 받아 무엇을 다음 단계로 넘기는지 포함한다.

**목표**
- 첫 번째 목표를 기술한다.
- 두 번째 목표를 기술한다.
- 세 번째 목표를 기술한다.

## 2. 개념

이 구현이 없다면 어떤 어려움이 생기는지 구체적인 예로 먼저 설명한다.
초보자가 처음 접하는 개념이라는 전제 아래, 직관적인 비유와 단계적 설명을 충분히 제공한다.

### 2.1. {{CONCEPT_1}}

핵심 개념을 한국어로 설명한다. 수식이 있는 경우 수식을 먼저 제시한 뒤 각 기호의 의미와 직관적 해석을 설명한다.

$$
{{FORMULA}}
$$

수식에서 $x$는 입력값을, $y$는 출력값을 나타낸다. 이 수식이 의미하는 바를 초보자 수준으로 풀어 설명한다.

핵심 용어는 다음과 같다.

| 용어 | 의미 | 이 프로젝트에서의 역할 |
|---|---|---|
| 용어 1 | 일반적인 의미 | 현재 구현에서 담당하는 책임 |
| 용어 2 | 일반적인 의미 | 다른 구성요소와의 연결 관계 |

### 2.2. {{CONCEPT_2}}

두 번째 핵심 개념을 설명한다. 수식 없는 Phase(batching, io 등)에서는 동작 원리를 서술체로 충분히 기술한다.

## 3. 구현

공개 인터페이스는 다음과 같다.

| 이름 | 종류 | 입력 | 출력 | 설명 |
|---|---|---|---|---|
| `{{API_NAME}}` | 함수/클래스 | `(N, D)` float32 | `(N, K)` float32 | 역할 한 줄 설명 |

### 3.1. {{SUBSECTION_1}}

구현의 첫 번째 핵심 부분을 설명한다. 개념 섹션의 수식·원리와 코드 변수명을 연결하여 해설한다.

```python
# 핵심 코드 스니펫
```

이 코드에서 `변수명`은 수식의 $기호$에 대응한다. 초보자가 놓치기 쉬운 비직관적인 구현 선택(수치 안정성, shape 변환, backend 선택 등)이 있다면 그 이유를 상세히 설명한다.

### 3.2. {{SUBSECTION_2}}

구현의 두 번째 핵심 부분을 설명한다. 서브섹션 수와 제목은 Phase 성격에 맞게 자유롭게 구성한다.

```python
# 핵심 코드 스니펫
```

## 4. 사용법

최소 사용 예제는 다음과 같다. import부터 출력까지 그대로 실행할 수 있는 코드를 제공한다.

```python
from {{MODULE_PATH}} import {{API_NAME}}

result = {{API_NAME}}(...)
print(result)
```

예상 출력은 다음과 같다.

```text
예상되는 type, shape 또는 출력값
```

프로젝트 통합 예제는 다음과 같다. 다른 모듈과 연결하는 실제 흐름을 보여준다.

```python
# 프로젝트 통합 흐름 예제
```

## 5. 테스트

테스트 파일은 `tests/{{STAGE_DIR}}/test_{{TOPIC}}.py`이다.

```bash
conda run -n {{ENV_NAME}} pytest tests/{{STAGE_DIR}}/test_{{TOPIC}}.py -v
```

테스트 구성은 다음과 같다.

| 클래스 | 항목 수 | 주요 검증 내용 |
|---|---|---|
| `Test{{ClassName}}` | N | shape 보존, 값 범위, 경계 조건, 상태 변화 |

## 6. 요약

이 Phase에서 구현한 내용과 핵심 설계 선택을 2~4문장으로 마무리한다.

다음 Phase에서는 [[{{NEXT_PHASE_LINK}}]]을 다룬다.
